/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Helpful when deploying on Vercel and reading generated files from /public
  poweredByHeader: false
};

export default nextConfig;
